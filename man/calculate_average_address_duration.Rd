% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{calculate_average_address_duration}
\alias{calculate_average_address_duration}
\title{Calculate Average Address Duration per Device and Time Period}
\usage{
calculate_average_address_duration(
  input_files,
  progress_interval = 10000L,
  device_filter = NULL,
  min_date = "",
  max_date = "",
  include_list = NULL,
  exclude_list = NULL,
  low_memory = FALSE,
  time_group = "day",
  exclude_addresses = NULL
)
}
\arguments{
\item{input_files}{Character vector of file paths to process.}

\item{progress_interval}{Integer. How often to print progress (0 = no progress). Default is 10000.}

\item{device_filter}{Character vector. Filter by specific device IDs. Empty = all devices.}

\item{min_date}{String. Minimum date in YYYYMMDD format. Empty = no minimum.}

\item{max_date}{String. Maximum date in YYYYMMDD format. Empty = no maximum.}

\item{include_list}{Character vector. Only include records where name starts with these prefixes.}

\item{exclude_list}{Character vector. Exclude records where name starts with these prefixes.}

\item{low_memory}{Logical. If TRUE, processes devices in batches (default 4 per pass, set via the \code{WSBT_DEVICES_PER_PASS} environment variable) to reduce peak memory, reading the input once per batch. Default is FALSE.}

\item{time_group}{String. Time grouping: "day" or "hour". Default is "day".}

\item{exclude_addresses}{Character vector. Exclude these addresses entirely (all packets). Empty = no filter.}
}
\value{
A data.frame with columns:
\describe{
\item{device}{Device ID}
\item{datetime}{Time period (YYYY-MM-DD for day, YYYY-MM-DD-HH for hour)}
\item{median_duration_seconds}{Median duration across all addresses in this period}
\item{mean_duration_seconds}{Mean duration across all addresses in this period}
\item{min_duration_seconds}{Minimum duration}
\item{max_duration_seconds}{Maximum duration}
\item{address_count}{Number of unique addresses detected}
\item{total_detections}{Total detection count}
}
}
\description{
Calculates statistics (median, mean, etc.) for address durations across all Bluetooth addresses
for each device grouped by time period (hour or day). This gives you the typical
detection duration for a device in each time window.
}
