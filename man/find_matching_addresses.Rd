% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{find_matching_addresses}
\alias{find_matching_addresses}
\title{Find Bluetooth addresses whose advertised name matches a prefix}
\usage{
find_matching_addresses(
  input_files,
  prefixes,
  progress_interval = 10000L,
  device_filter = NULL,
  min_date = "",
  max_date = ""
)
}
\arguments{
\item{input_files}{Character vector of file paths to process.}

\item{prefixes}{Character vector of name prefixes to match.}

\item{progress_interval}{Integer. How often to print progress (0 = no progress).}

\item{device_filter}{Character vector. Restrict to these device IDs. Empty = all.}

\item{min_date}{String. Minimum date in YYYYMMDD format. Empty = no minimum.}

\item{max_date}{String. Maximum date in YYYYMMDD format. Empty = no maximum.}
}
\value{
A data.frame with one row per matched address: \code{address},
\code{name}, \code{matched_prefix}, \code{hits}, \code{first_seen},
\code{last_seen}.
}
\description{
Single pass over the raw data. For every record whose advertised name starts
with one of \code{prefixes}, the address is recorded together with the prefix
that matched. This promotes a per-packet name match to a per-address match, so
that downstream steps can act on \emph{all} packets (named or not) for a
device identified as a beacon, vehicle, bike, etc.
}
\keyword{internal}
